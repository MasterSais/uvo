  - CI/CD service (gitlab e.g.)

  - Promise validator

  - 'uvo/extended' validators e.g https://www.npmjs.com/package/class-validator#manual-validation

  - Templating api should accept Vbase as parameter

  - Templating api CJS module

  - Templating api

    '#value(@string)'; // set validator to 'value' dep

    '##value'; // force fetching from meta to val. seq.

    '@compare(>$0)!0 : @compare(<=$1)!0'; -> '@compare(>$0,<=$1)!0';

    `
      @object(
        $0 @number $1
      )
    `;

    'defined'; //??? m.b use only long name. Short only via 'compare'. m.b. defined | def

    'empty'; //??? m.b. empty | emp

    'fields'; // fields | f

    'is'; // is | i

    'unique'; // unique | u

    'regex'; // regex | r or 'compare(*$0)'

    'keysMap'; // keysMap | km

    'valueMap'; // valueMap | vm

    'consecutive'; // @seq

    'parallel'; // @prll

    'withFallback'; // ~f(123)

    'useDefault'; // @default(123)

    'dynamic'; // $... in seq

    'or'; // @or

    'strip'; // strip | stp ???