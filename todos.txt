  - CI/CD service (gitlab e.g.)

  - Promise validator

  - 'uvo/extended' validators e.g https://www.npmjs.com/package/class-validator#manual-validation

  - Templating api CJS module

  - Docs. Examples to I block, then Api

  - Templating optimize. Make all inj's callable on injection.

  - Docs 'All Features' block with all validators and features in one schema. ???

  - Templating api

    `
      @object(
        $0 @number
      )
    `;

    ~m($0); // logger

    ~e($0); // common error processor

    'defined'; //??? m.b use only long name. Short only via 'compare'. m.b. defined | def

    'empty'; //??? m.b. empty | emp

    'fields'; // fields | f

    'is'; // is | i

    'unique'; // unique | u

    'regex'; // regex | r or 'compare(*$0)'

    'keysMap'; // keysMap | km

    'valueMap'; // valueMap | vm

    'or'; // [@number : @string]

    'parallel'; // {@number : @string} ???

    'withFallback'; // ~f(123)

    'useDefault'; // @default(123)

    'strip'; // strip | stp ???