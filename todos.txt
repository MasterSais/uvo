v2: 
  - Think of validator's error value
  - defined, empty => oneOf([undefined, ...]) EMPTY_VALUES
  - gte, lte, equal => compare('type') types: lt, gt, lte, gte, equal, deepEqual
  - len, minLen, maxLen => length('type') types: lt, gt, lte, gte, equal
  - number.integer() => number.multiple(1)

minification:
  - const => let

v*:
  - compile()