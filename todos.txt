  - CI/CD service (gitlab e.g.)

  - 'uvo/extended' validators e.g https://www.npmjs.com/package/class-validator#manual-validation

  - Templating api CJS module

  - Test coverage into Doc

  - setRef, getRef

  - Checkable ? rm

  - Perf tests

  - Dynamic should accept value

  - Templating api

    'fields'; // @fields( &.[^.id.guid].name )

    'unique'; // unique | u

    'keysMap'; // keysMap | km

    'valueMap'; // valueMap | vm

    'withFallback'; // ~f(123)

    'useDefault'; // @default(123)

    'strip'; // strip | stp ???